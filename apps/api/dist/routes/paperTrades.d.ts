/**
 * Paper Trades & Execution Logs routes — per-user, per-strategy.
 * All data stored in MongoDB so it's isolated per account.
 */
import type { FastifyInstance } from "fastify";
export declare function registerPaperTradeRoutes(app: FastifyInstance): Promise<void>;
//# sourceMappingURL=paperTrades.d.ts.map