{
  "name": "polyblocks",
  "version": "0.1.0",
  "private": true,
  "description": "No-code Polymarket strategy builder",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "start": "node apps/api/dist/server.js",
    "lint": "turbo lint",
    "clean": "turbo clean",
    "heroku-postbuild": "pnpm run clean && pnpm run build"
  },
  "engines": {
    "node": "22.x"
  },
  "devDependencies": {
    "turbo": "^2.4.0",
    "typescript": "^5.7.0"
  },
  "packageManager": "pnpm@9.15.0"
}

